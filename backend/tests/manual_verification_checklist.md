# Phase C 手动验收清单

> 需要浏览器测试的用例。pytest 覆盖了 API 层，这些用例需要真实 UI 验证。

## 前置条件

1. 启动后端：`uvicorn app.main:app --reload`（不推荐用 `--reload`，Windows 上有 Playwright subprocess 问题）
2. 启动前端：`cd frontend && npm run dev`
3. 打开浏览器：`http://localhost:3000`

---

## C-00: 登录与 Cookie 认证

**预期**：登录成功后响应设置 `pm_access_token`、`pm_refresh_token`、`pm_csrf_token`；刷新页面后仍通过 `/auth/me` 恢复登录态。

**验证步骤**：

1. 访问 `http://localhost:3000/login`
2. 使用测试用户 `default123` / `123456` 登录
3. 在 DevTools Application/Cookies 中确认三类 Cookie 存在
4. 刷新页面，确认不会回到登录页
5. Network 面板确认初始化请求为 `/api/v1/auth/me`

**证据**：截图 `C-00-cookie-auth.png`

**结果**：✅ 通过 / ❌ 失败

```
实际行为：
```

---

## C-01: 前端默认入口

**预期**：访问 `http://localhost:3000` 自动进入职位管理页（路由 `/jobs`）

**验证步骤**：

1. 清空浏览器地址栏，直接访问 `http://localhost:3000`
2. 观察页面是否直接显示职位管理内容（不是空白页或错误页）

**证据**：截图 `C-01-frontend-default-entry.png`

**结果**：✅ 通过 / ❌ 失败

```
实际行为：
```

---

## C-02: 侧边栏常驻 + 路由切换

**预期**：左侧菜单始终可见，点击"数据看板"、"职位管理"、"商品管理"和"定时配置"时右侧区域切换，左侧不变

**验证步骤**：

1. 确认左侧有"数据看板"、"职位管理"、"商品管理"和"定时配置"菜单项
2. 点击"数据看板"，确认 KPI 卡片或趋势图可见
3. 点击"定时配置"，左侧保持不变，右侧显示配置表单
4. 点击"商品管理"，返回商品列表

**证据**：截图 `C-02-sidebar-navigation.png`（包含切换前后对比）

**结果**：✅ 通过 / ❌ 失败

```
实际行为：
```

---

## C-02b: Dashboard KPI / SSE

**预期**：Dashboard 初次进入即加载 KPI 数据，Network 中存在 `/api/v1/dashboard/kpi`、`/api/v1/dashboard/trends`，SSE 连接 `/api/v1/dashboard/events` 不携带 query token。

**验证步骤**：

1. 访问 `/dashboard`
2. 观察 KPI 卡片、趋势图、平台/薪资分布是否加载
3. Network 面板筛选 `dashboard`，确认请求路径为 `/api/v1/dashboard/*`
4. 等待 SSE 初始推送，确认无 console error

**证据**：截图 `C-02b-dashboard.png`

**结果**：✅ 通过 / ❌ 失败

```
实际行为：
```

---

## C-03: 商品全流程（完整 UI 验证）

### C-03a: 单条新增

**验证步骤**：

1. 点击"新增"按钮
2. 填写 platform=jtaobao、url=https://item.taobao.com/item.htm?id=1234567890、title=测试商品
3. 点击确认
4. 观察商品是否出现在列表

**证据**：截图 `C-03a-create-success.png`

### C-03b: 单条编辑

**验证步骤**：

1. 点击列表中任意商品的"编辑"按钮
2. 修改 title
3. 确认修改成功，列表中 title 已更新

**证据**：截图 `C-03b-edit-success.png`

### C-03c: 单条删除

**验证步骤**：

1. 点击商品行的"删除"按钮
2. 确认二次确认弹窗出现
3. 确认删除后商品从列表消失

**证据**：截图 `C-03c-delete-success.png`

### C-03d: 批量导入

**验证步骤**：

1. 点击"批量导入"
2. 粘贴多个 URL（包含重复）
3. 观察平台识别结果
4. 确认导入完成后显示成功/失败汇总

**证据**：截图 `C-03d-batch-import.png`

### C-03e: 批量删除

**验证步骤**：

1. 选中 2-3 个商品
2. 点击"批量删除"
3. 确认二次确认
4. 观察删除结果汇总

**证据**：截图 `C-03e-batch-delete.png`

### C-03f: 批量启停

**验证步骤**：

1. 选中商品
2. 点击"批量停用"
3. 确认状态列已更新

**结果**：✅ 通过 / ❌ 失败（每个子项）

```
C-03a: ✅ / ❌
C-03b: ✅ / ❌
C-03c: ✅ / ❌
C-03d: ✅ / ❌
C-03e: ✅ / ❌
C-03f: ✅ / ❌
```

---

## C-04: 服务端分页

### C-04a: 分页每页 15 条

**验证步骤**：

1. 确认商品数量 >= 16
2. 观察第一页显示的商品数量
3. Network 面板确认请求参数包含 `page=1&size=15`

**证据**：截图 `C-04a-pagination-15-per-page.png`（含 Network 请求）

### C-04b: 翻页后刷新列表

**验证步骤**：

1. 点击"下一页"
2. 观察商品列表是否变化
3. Network 面板确认新请求带 `page=2`

**证据**：截图 `C-04b-next-page.png`

### C-04c: 筛选 + 分页联动

**验证步骤**：

1. 选择平台筛选（如"淘宝"）
2. 观察列表是否只显示淘宝商品
3. 点击翻页，筛选条件保持

**结果**：✅ 通过 / ❌ 失败

```
C-04a: ✅ / ❌
C-04b: ✅ / ❌
C-04c: ✅ / ❌
```

---

## C-05: 定时配置真实读写

### C-05a: GET 展示后端值

**验证步骤**：

1. 先用 curl 确认后端当前 cron 值：`curl http://127.0.0.1:8000/config`
2. 访问定时配置页
3. 观察页面显示的 cron 值是否与后端一致

**证据**：截图 `C-05a-get-backend-value.png`

### C-05b: PATCH 保存生效

**验证步骤**：

1. 修改 cron 表达式（如改为 `0 10 * * *`）
2. 点击保存
3. 用 curl 重新 GET `/config`，确认值已更新

**证据**：截图 `C-05b-patch-saved.png` + curl 输出

### C-05c: 草稿兜底 + 恢复

**验证步骤**：

1. 修改 cron 但不保存
2. 刷新页面（F5）
3. 观察是否出现"检测到未保存草稿"提示
4. 点击"恢复草稿"，确认值恢复

**证据**：截图 `C-05c-draft-recovery.png`

**结果**：✅ 通过 / ❌ 失败

```
C-05a: ✅ / ❌
C-05b: ✅ / ❌
C-05c: ✅ / ❌
```

---

## C-06 & C-07: APScheduler 调度

### C-06a: Scheduler 状态端点

**验证步骤**：

1. `curl http://127.0.0.1:8000/scheduler/status`
2. 确认返回 JSON 包含 `scheduler`、`job_registered`、`next_run_at`

**证据**：curl 输出 + 截图

### C-06b: Cron 热更新后 job 被替换

**验证步骤**：

1. `curl http://127.0.0.1:8000/scheduler/status` 记录当前 job 信息
2. 修改 cron 为新值（如从 `0 9 * * *` 改为 `0 12 * * *`）
3. 再次 GET `/scheduler/status`，确认 `next_run_at` 变化

**证据**：截图 `C-06b-job-rebuilt.png`（更新前后对比）

### C-06c: 重叠执行时跳过

**验证步骤**：

1. 这项需要实际触发爬取或查看日志
2. 观察日志中是否有 "Crawl skipped: another crawl is in progress" 或 "Crawl started"

**证据**：后端日志截图

**结果**：✅ 通过 / ❌ 失败

```
C-06a: ✅ / ❌
C-06b: ✅ / ❌
C-06c: ✅ / ❌
```

---

## C-E05: 删除后当前页为空时自动回退

**预期**：在最后一页删除所有商品后，页面自动回到上一页

**验证步骤**：

1. 确认商品总数，计算最后一页位置
2. 跳转到最后一页
3. 全选当前页所有商品
4. 点击"批量删除"
5. 确认删除后自动回退到上一页

**证据**：截图序列 `C-E05-rollback-step1.png` ~ `C-E05-rollback-step3.png`

**结果**：✅ 通过 / ❌ 失败

```
实际行为：
```

---

## C-E08: 5xx/超时可重试提示

**预期**：后端返回 5xx 或超时时，前端显示可重试的提示（非静默失败）

**验证步骤**（两种方式）：

### 方式 A：关掉后端触发超时

1. 停止后端服务
2. 点击"保存配置"
3. 观察前端错误提示

### 方式 B：检查 axios interceptor

1. 检查 `frontend/src/api/client.ts` 中是否有 5xx 错误处理
2. 检查调用处（`ScheduleConfigPage.tsx`）是否有重试 UI

**证据**：截图 `C-E08-retry-ui.png` 或代码截图

**结果**：✅ 通过 / ❌ 失败

```
实际行为：
```

---

## 证据汇总表

| 编号  | 场景               | 结果    | 证据文件     |
| ----- | ------------------ | ------- | ------------ |
| C-00  | Cookie 登录        | ✅ / ❌ | C-00-\*.png  |
| C-01  | 前端默认入口       | ✅ / ❌ | C-01-\*.png  |
| C-02  | 侧边栏路由         | ✅ / ❌ | C-02-\*.png  |
| C-02b | Dashboard/SSE      | ✅ / ❌ | C-02b-\*.png |
| C-03a | 单条新增           | ✅ / ❌ | C-03a-\*.png |
| C-03b | 单条编辑           | ✅ / ❌ | C-03b-\*.png |
| C-03c | 单条删除           | ✅ / ❌ | C-03c-\*.png |
| C-03d | 批量导入           | ✅ / ❌ | C-03d-\*.png |
| C-03e | 批量删除           | ✅ / ❌ | C-03e-\*.png |
| C-03f | 批量启停           | ✅ / ❌ | C-03f-\*.png |
| C-04a | 分页 15 条         | ✅ / ❌ | C-04a-\*.png |
| C-04b | 翻页               | ✅ / ❌ | C-04b-\*.png |
| C-04c | 筛选+分页          | ✅ / ❌ | C-04c-\*.png |
| C-05a | GET 展示后端值     | ✅ / ❌ | C-05a-\*.png |
| C-05b | PATCH 保存生效     | ✅ / ❌ | C-05b-\*.png |
| C-05c | 草稿恢复           | ✅ / ❌ | C-05c-\*.png |
| C-06a | Scheduler 状态 API | ✅ / ❌ | C-06a-\*.png |
| C-06b | Job 热更新         | ✅ / ❌ | C-06b-\*.png |
| C-06c | 重叠执行跳过       | ✅ / ❌ | C-06c-\*.png |
| C-E05 | 删除回退           | ✅ / ❌ | C-E05-\*.png |
| C-E08 | 5xx 重试提示       | ✅ / ❌ | C-E08-\*.png |

---

## 最终验收结论

**测试执行日期**：
**测试环境**：后端 `http://127.0.0.1:8000` + 前端 `http://localhost:3000`

| 类别                    | 总用例 | 通过 | 失败 |
| ----------------------- | ------ | ---- | ---- |
| 联调清单（C-01~C-10）   | 10     |      |      |
| 边界专项（C-E01~C-E09） | 9      |      |      |
| 回归清单（C-R01~C-R03） | 3      |      |      |
| **合计**                | **22** |      |      |

**发布结论**：✅ 可发布 / ❌ 不可发布

**阻塞问题**（P0/P1）：

1.
2.

**待修复问题**（P2/P3）：

1.
2.
